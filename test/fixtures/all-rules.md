No top-level heading here so MD041-fix will add one.

**Summary**

A long paragraph that exceeds eighty characters and should be wrapped when you run the fix.

- A list item that is also way too long and should be wrapped onto the next line.

> Blockquote that is far too long and should be wrapped by the line length rule.

Inline <strong>HTML</strong> and <em>here</em>. Link: <a href="https://example.com">example</a>.

Empty links: [click]() and [](#).

```
code without language
```

Last line with no newline at end.
